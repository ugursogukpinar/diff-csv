version: '3.2'


services:
    app:
      build:
        context: ./
        dockerfile: Dockerfile
      volumes:
        - ./diffcsv:/app
        - ./data:/usr/src/data
      working_dir: /app
      